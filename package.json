{
  "name": "ngo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "prettier": "^2.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format"
    }
  }
}
